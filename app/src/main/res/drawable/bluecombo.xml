<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient android:startColor="@color/colorSkyBlue"
        android:endColor="@color/colorWhite"
        android:angle="0"
        android:type="linear"
        android:centerColor="@color/colorSkyBlue"></gradient>

</shape>